{
  "name": "tsdk",
  "version": "0.0.19-beta.8",
  "main": "lib/index.js",
  "bin": {
    "tsdk": "bin/tsdk.js"
  },
  "scripts": {
    "start": "npm run build && npx pnpm --filter server-example sync && npx pnpm --filter server-example build && npx pnpm --filter server-example example",
    "start-publish": "npm run build && npm publish --registry=https://registry.npmjs.org",
    "build": "rm -rf lib && tsc --project tsconfig.json",
    "start-website": "cd website && yarn && npm run start",
    "build-website": "cd website && yarn && npm run build",
    "test": "echo tests",
    "lint": "eslint . --ext .ts,.tsx --fix"
  },
  "files": [
    "bin/*",
    "lib/*",
    "fe-sdk-template/*",
    "README.md"
  ],
  "keywords": [
    "TypeScript",
    "Share Code",
    "APIs",
    "typeorm",
    "zod",
    "express",
    "@nestjs/cli",
    "tsdk",
    "http",
    "express",
    "ws",
    "socket.io"
  ],
  "dependencies": {
    "chalk": "4.1.2",
    "fast-glob": "^3.2.12",
    "fs-extra": "^10.1.0"
  },
  "devDependencies": {
    "@acme/eslint-config": "*",
    "@acme/ts-config": "*",
    "@types/fs-extra": "^9.0.13",
    "@types/node": "^18.7.16",
    "@types/webpack-node-externals": "^2.5.3",
    "webpack": "^5.74.0",
    "webpack-node-externals": "^3.0.0"
  },
  "eslintConfig": {
    "extends": [
      "@acme/eslint-config/node"
    ],
    "ignorePatterns": [
      "node_modules"
    ]
  },
  "license": "MIT"
}
